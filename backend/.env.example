PORT=5001
MONGO_URI=mongodb://localhost:27017/thinksheet
JWT_SECRET=your_jwt_secret_here

# Streamline (think-book) backend URL — used for server-to-server calls if needed
STREAMLINE_API_URL=http://localhost:5000/api
